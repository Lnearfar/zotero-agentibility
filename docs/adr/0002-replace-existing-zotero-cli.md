# Replace the existing zotero-cli

This project owns the `zotero-cli` executable and does not preserve command compatibility with the CLI shipped by `zotero-mcp-server`. The shorter `zotero` executable is not used because it already launches the Zotero desktop application on the target system.
